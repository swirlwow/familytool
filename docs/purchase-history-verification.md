# 購買紀錄實作與驗收

更新：2026-09-16。實作與本機隔離驗收完成；尚未提交、推送、部署或變更正式資料庫。

## 範圍

- 待購清單／購買紀錄分頁，保留原待購来源。
- 完成購買表單、直接新增、修改、刪除及選擇恢復待購。
- 再次加入待購，保留原購買快照；不沿用舊實付金額。
- 品名、規格、店家、備註與日期查詢；未知金額不當成零。
- 舊已購項目標示日期／金額待補，不推測歷史資料。
- 家庭備份 v3 包含購買紀錄；驗證器仍接受完整 v2 備份。
- 工作區權限、更新欄位白名單、原子操作、重送鍵。
- 不自動建立記帳；未修改其他功能。

## 驗收結果

- Vitest：24 個測試檔、180 項測試通過。
- TypeScript `--noEmit`、ESLint `src --quiet` 通過。
- 隔離環境 Next.js 正式模式建置通過。
- 24 項已登入既有 API 回歸檢查通過。
- `supabase/tests/shopping_purchases.sql` 真實 Postgres 整合測試通過：舊狀態相容、完成購買、重送、快照、來源保留、再次購買、備份、刪除／恢復、無記帳寫入、工作區及匿名隔離。測試交易全部 rollback。
- Supabase 本機 security advisors：No issues found。
- 瀏覽器實測：直接新增留空金額、修改為 123.45、再次加入待購、待購轉購買、帶入來源、刪除並恢復待購。原紀錄與新紀錄獨立，總額提示有顯示未填筆數。
- 390 × 844 手機表單檢查：對話框 x=12、width=366、right=378，無橫向溢出；可捲動欄位及使用儲存按鈕。桌面與手機清單截图已留在本機驗收目錄。
- 同一 request key 並行 POST 兩次，回傳同一紀錄 ID。
- 實際 `/api/export` 回傳 v3，包含購買紀錄，123.45 金額與 1.25 數量精度保留；外部工作區 API 回應 403。
- 購買三表型別化隔離還原通過：ID、來源外鍵、金額／數量、null、刪除狀態一致，演練全部 rollback。

## 清理

驗收使用 `phase2-clean-family` 空白本機庫及新合成帳號。完成後登出並移除合成帳號、資料，關閉驗收伺服器與專用瀏覽器。
清理確認：auth.users=0、auth.sessions=0、workspaces=0、shopping_purchases=0。
新 schema 僅套用本機測試庫。正式環境未變更。

## 環境問題與處理

重開機後 Docker 引擎恢復。原驗收連接埠 55563 被 SHARP 掃描器服務占用，改用 55663，未停止或修改掃描器。

## 發布與還原注意

1. 取得發布授權後，先備份並套用 `20260915084734_shopping_purchase_records.sql`，再發布前端/API；不得漏套表、RPC、RLS、備份函式。
2. 精確還原 v3 待購／購買快照時，需在受控管理員交易內暫停 `shopping_purchase_compatibility` 這一個相容觸發器，按待購、來源、購買順序還原後立即啟用。否則還原「已購買」待購項目會自動產生另一份舊格式快照。外鍵保持啟用。
3. 本次未新增一般使用者自行還原 UI。既有備份驗證相容性保留。

驗收腳本及畫面：`D:/@WORK/Shift Schedule/purchase-history-2026-09-16/`。
